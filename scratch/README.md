# scratch/ - this folder is for throwaway files and temporary WIP files.

 — throwaway local experiments

This folder is **not shipped**.

The old Step 1.0d API throwaway harness has been retired now that the tracked
Stage 1 proof lives under `tests/harness/stage1_smoke.py`.

Only keep short-lived local experiments here. If a script becomes part of the
project's repeatable validation surface, move it into `tests/harness/` or the
shared `src/pyocd_debug_mcp/` service layer.
