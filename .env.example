# Copy to .env and edit for your machine. .env is gitignored.
# Both are optional — omit to let pyOCD auto-detect a single attached probe.

# Whole or partial probe serial / unique ID (see `uv run pyocd list`).
PYOCD_PROBE_UID=

# Target type override, e.g. stm32f407vg, nrf52840.
PYOCD_TARGET=
