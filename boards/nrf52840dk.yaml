board_id: nrf52840dk  # PROJECT-DEFINED (our canonical board id)
display_name: "nRF52840-DK"  # PROJECT-DEFINED (human-facing board label)
mcu_family: nrf52840  # HW-FIXED (the silicon on the reference board)
probe_family: jlink  # HW-FIXED (the onboard debug probe family)
pyocd_target: nrf52840  # VENDOR-FIXED, UNVERIFIED (confirm via `pyocd list --targets`)
serial_baudrate: 115200  # PROJECT-DEFINED, UNVERIFIED (chosen reference-firmware UART default)
test_read_address: 0x10000000  # HW-FIXED (nRF FICR base; safe readable smoke-test address)
pack_name: nrf52840  # VENDOR-FIXED, UNVERIFIED (`pyocd pack install` search/install token)
probe_hint_terms:  # PROJECT-DEFINED, UNVERIFIED (host-side probe matching heuristics)
  - j-link
  - jlink
  - segger
  - nrf
serial_hint_terms:  # PROJECT-DEFINED, UNVERIFIED (host-side serial-port matching heuristics)
  - j-link
  - jlink
  - segger
  - nrf
  - virtual com
reference_firmware_path: ../firmware/nrf52840dk/reference/build/firmware.elf  # PROJECT-DEFINED, UNVERIFIED (repo-owned Stage 0 baseline artifact path; not a user's project path)
requires_recover_validation: true  # PROJECT-DEFINED (Stage 0 policy for this board family)
recover_command: "nrf recover"  # VENDOR-FIXED, UNVERIFIED (pyOCD command string for unlock/recover)
# Do not add user project paths, build commands, or output artifact paths here.
# Those are runtime/session inputs, not board metadata.
