board_id: my_custom_board  # PROJECT-DEFINED, UNVERIFIED (example canonical board id; rename for your board)
display_name: "My Custom Board"  # PROJECT-DEFINED, UNVERIFIED (example display name)
mcu_family: rp2040  # HW-FIXED, UNVERIFIED (confirm against the actual board BOM / datasheet)
probe_family: cmsisdap  # HW-FIXED, UNVERIFIED (confirm the actual attached probe family)
pyocd_target: rp2040  # VENDOR-FIXED, UNVERIFIED (confirm via `pyocd list --targets`)
serial_baudrate: 115200  # PROJECT-DEFINED, UNVERIFIED (example reference-firmware UART default)
test_read_address: 0x20000000  # HW-FIXED, UNVERIFIED (example readable SRAM address; confirm for your target)
pack_name: rp2040  # VENDOR-FIXED, UNVERIFIED (`pyocd pack install` search/install token)
probe_hint_terms:  # PROJECT-DEFINED, UNVERIFIED (example host-side probe matching heuristics)
  - cmsis-dap
  - daplink
  - myboard
serial_hint_terms:  # PROJECT-DEFINED, UNVERIFIED (example host-side serial-port matching heuristics)
  - myboard
  - usb serial
  - uart
reference_uart_patterns:  # PROJECT-DEFINED, UNVERIFIED (example expected UART validation text)
  - "hello"
# Do not add firmware paths, user project paths, build commands, or output artifact paths here.
# Those are runtime/session inputs supplied by the host, not board metadata.
