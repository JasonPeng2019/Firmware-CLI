board_id: nucleo_l476rg  # PROJECT-DEFINED (our canonical board id)
display_name: "Nucleo-L476RG"  # PROJECT-DEFINED (human-facing board label)
mcu_family: stm32l476  # HW-FIXED (the silicon family on the reference board)
probe_family: stlink  # HW-FIXED (the onboard debug probe family)
pyocd_target: stm32l476rg  # VENDOR-FIXED, UNVERIFIED (confirm via `pyocd list --targets`)
serial_baudrate: 115200  # PROJECT-DEFINED, UNVERIFIED (chosen reference-firmware UART default)
test_read_address: 0x08000000  # HW-FIXED (STM32 flash base; safe readable smoke-test address)
pack_name: stm32l476  # VENDOR-FIXED, UNVERIFIED (`pyocd pack install` search/install token)
uart_note: "Reference firmware should print on USART2 at 115200 8N1."  # PROJECT-DEFINED, UNVERIFIED (expected Stage 0 validation note)
probe_hint_terms:  # PROJECT-DEFINED, UNVERIFIED (host-side probe matching heuristics)
  - st-link
  - stlink
  - stm32
  - nucleo
serial_hint_terms:  # PROJECT-DEFINED, UNVERIFIED (host-side serial-port matching heuristics)
  - st-link
  - stlink
  - stm32
  - nucleo
  - virtual com
# Do not add firmware paths, user project paths, build commands, or output artifact paths here.
# Those are runtime/session inputs supplied by the host, not board metadata.
